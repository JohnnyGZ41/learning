spring.datasource.url=jdbc:mysql://localhost:8080/lunaris?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true

# Tu usuario de MySQL (por defecto suele ser root)
spring.datasource.username=root

# Tu contrase�a de MySQL (c�mbiala si es diferente, a veces es 'root', 'mysql' o vac�a)
spring.datasource.password=undertale41

# Driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Mostrar las consultas SQL en la consola (�til para ver qu� est� haciendo)
spring.jpa.show-sql=true